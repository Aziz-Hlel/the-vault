
---

- add file `.gitattributes` :
```
* text=auto eol=lf
```
- to make sure Git automatically detects text files and normalizes their line endings using **LF (`\n`)**, regardless of whether you're on Windows, macOS, or Linux.